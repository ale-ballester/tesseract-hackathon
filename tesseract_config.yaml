# Tesseract configuration file
# Generated by tesseract 1.1.1 on 2026-01-03T23:54:02.020022

name: "pic-rom-optimization"
version: "1.0.0"
description: "PIC-ROM optimization with Fourier actuator control for plasma simulation"

build_config:
  # Base image to use for the container, must be Ubuntu or Debian-based
  # base_image: "debian:bookworm-slim"

  # Platform to build the container for. In general, images can only be executed
  # on the platform they were built for.
  target_platform: "native"

  # Additional packages to install in the container (via apt-get)
  extra_packages:
    - ffmpeg

  # Data to copy into the container, relative to the project root
  package_data:
    - [pic_simulation.py, /tesseract/pic_simulation.py]
    - [control.py, /tesseract/control.py]
    - [optimize.py, /tesseract/optimize.py]
    - [utils.py, /tesseract/utils.py]
    - [plotting.py, /tesseract/plotting.py]

  # Additional Dockerfile commands to run during the build process
  custom_build_steps:
    - |
      ENV PYTHONPATH=/tesseract:${PYTHONPATH:-}